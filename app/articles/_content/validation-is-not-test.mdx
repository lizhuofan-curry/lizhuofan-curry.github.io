## 名称决定结论边界

验证集用于训练过程中的模型选择和调参。测试集则应尽量留到最终评估。如果训练过程中已经反复观察某一份数据的结果，它就不再承担完全独立的测试角色。

## 图表也需要来源

一条准确率曲线至少要能回答这些问题：它来自哪个数据划分、使用什么训练设置、横轴和纵轴分别是什么。如果只有验证曲线，就应明确写“验证准确率”，而不是改名为“测试准确率”。

## 让实验可追溯

我会尽量保留数据划分、随机种子、训练轮数、模型版本和绘图脚本。数字的价值不仅在于高低，也在于别人能否理解它是怎样得到的。

诚实地缩小结论范围，通常比给结果贴上更响亮的名字更有用。
