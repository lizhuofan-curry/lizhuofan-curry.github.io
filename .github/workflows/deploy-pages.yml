name: Legacy GitHub Pages deployment (retired)

on: workflow_dispatch

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Explain retirement
        run: |
          echo "GitHub Pages deployment is retired."
          echo "This full-stack application requires a Node.js runtime, PostgreSQL, Better Auth, media storage, and email delivery."
          echo "Use Vercel today or docs/SELF_HOSTING.md for a future self-hosted deployment."
